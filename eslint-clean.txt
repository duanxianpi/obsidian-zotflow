# Paths that are already ESLint-clean and must stay that way.
#
# `npm run lint:clean` runs ESLint over exactly these paths with
# `--max-warnings 0`, and it runs as part of `npm test`.
#
# The backlog is now down to four files, all held back by a decision rather
# than by effort — see the note at the bottom. Everything else is locked as a
# directory, so a new file starts out held to the same bar instead of quietly
# joining the backlog.
#
# One path per line. Blank lines and `#` comments are ignored.

scripts
tests
eslint.config.mts
vitest.config.ts

src/bridge
src/bundle-assets
src/db
src/services
src/types
src/ui
src/utils
src/worker

src/settings/types.ts
src/settings/sections/citation-section.ts
src/settings/sections/csl-section.ts
src/settings/sections/general-section.ts
src/settings/sections/sync-section.ts

# Deliberately NOT listed, and why:
#
#   src/settings/settings.ts          `display()` is deprecated in favour of
#   src/settings/sections/cache-section.ts    the 1.13 declarative settings API,
#   src/settings/sections/webdav-section.ts   and `setWarning()` in favour of
#                                     `setDestructive()` — which is @since
#                                     1.13.0, above this plugin's
#                                     minAppVersion of 1.11.4. Adopting either
#                                     is a compatibility decision, not a
#                                     cleanup.
#
#   src/main.ts                       Ships a default hotkey for
#                                     "Insert Citation". Obsidian's guideline
#                                     says not to; removing it takes a working
#                                     shortcut away from existing users.
